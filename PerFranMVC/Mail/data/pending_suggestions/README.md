# Pending Suggestions Storage

This directory stores temporary data for quiz suggestions awaiting admin approval.
Each suggestion is stored as a JSON file with a unique token as the filename.
Files are automatically cleaned up after 7 days or after admin approval/rejection.
